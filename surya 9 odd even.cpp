#include <stdio.h>

int main() 
{ 
     printf("Surya chawla\n");
	 int a;
	 printf("enter the number");
	 scanf("%d",&a);
	 if(a%2==0)
	 { 
	 printf(" it is an even number");
	 }
	 else
	 {
	 printf("it is an odd number"); 
	 }	 
	 return (0);
}
